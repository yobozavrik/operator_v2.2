'use client';

import React from 'react';
import { SadovaDistributionPanel } from '../../components/sadova/SadovaDistributionPanel';

export default function SadovaPage() {
    return (
        <main className="flex h-full w-full flex-col p-6">
            <SadovaDistributionPanel />
        </main>
    );
}
